
import { atom } from 'recoil';

export const name = atom({
    key: "name",
    default: ""
})

export const busName = atom({
    key: "businessName",
    default: ""
})

export const email = atom({
    key: "email",
    default: ""
})

export const imageUrl = atom({
    key: "imageUrl",
    default: ""
})